import React from 'react';
import { NavigationBar } from '../navigationBar/NavigationBar'

export const Header = () => {
    return (
        <header class="header">
            <NavigationBar />
        </header>
    )
}